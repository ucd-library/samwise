apiVersion: v1
kind: Service
metadata:
  name: vllm
spec:
  selector:
    app: cyberdyne02-qwen-embed-vllm
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: 8001
      nodePort: 30803
