apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
spec:
  template:
    spec:
      containers:
        - name: postgres
          # Pin to a specific minor version in prod — update per release
          image: postgres:16
      nodeSelector:
        intended-for: "samwise-frontend"